[webservers]
%{ for instance in webservers ~}
${instance.name} ansible_host=${instance.network_interface.0.nat_ip_address} fqdn=${instance.fqdn}
%{ endfor ~}

[databases]
%{ for instance in databases ~}
${instance.name} ansible_host=${instance.network_interface.0.nat_ip_address} fqdn=${instance.fqdn}
%{ endfor ~}

[storagevm]
%{ for instance in storagevm ~}
${instance.name} ansible_host=${instance.network_interface.0.nat_ip_address} fqdn=${instance.fqdn}
%{ endfor ~}